export function Simulate() {
  return (
    <section className="section" id="simulate">
      <div className="wrap">
        <div className="section-kicker">③ Run the experiment</div>
        <h2>On May 2, 2026, Spirit Airlines stopped flying</h2>
        <p className="section-lede">
          A major low-cost carrier exited hundreds of routes at once. What
          should happen to fares — under each camp's numbers?
        </p>
        <div className="placeholder-note">
          The scenario engine lands in Phase 5 — the Spirit shutdown and
          "Big Three divest" scenarios, with the prediction itemized like an
          airline receipt (including the disputed line item).
        </div>
      </div>
    </section>
  );
}
