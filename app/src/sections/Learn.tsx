export function Learn() {
  return (
    <section className="section" id="learn">
      <div className="wrap">
        <div className="section-kicker">① Learn the mechanism</div>
        <h2>Two airlines, one owner — sixty seconds</h2>
        <p className="section-lede">
          Before the real data: a toy market you can push on. Drag ownership
          around and watch which concentration score moves.
        </p>
        <div className="placeholder-note">
          The interactive toy model lands in Phase 6 — the slider-driven
          ownership web (two imaginary airlines; HHI sits still while MHHI
          delta climbs).
        </div>
      </div>
    </section>
  );
}
